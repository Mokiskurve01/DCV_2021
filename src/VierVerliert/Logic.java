package VierVerliert;

public class Logic {

    public boolean check(Position position, Field field, boolean pruefung) {

       /* if (field.board[Integer.parseInt(position.position)][0].equals(field.board[Integer.parseInt(position.position)][0])) {
            pruefung = true;
        } else {
            pruefung = false;
        }
        */

        return pruefung;
    }
}
